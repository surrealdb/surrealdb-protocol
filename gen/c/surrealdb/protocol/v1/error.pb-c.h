/* Generated by the protocol buffer compiler.  DO NOT EDIT! */
/* Generated from: surrealdb/protocol/v1/error.proto */

#ifndef PROTOBUF_C_surrealdb_2fprotocol_2fv1_2ferror_2eproto__INCLUDED
#define PROTOBUF_C_surrealdb_2fprotocol_2fv1_2ferror_2eproto__INCLUDED

#include <protobuf-c/protobuf-c.h>

PROTOBUF_C__BEGIN_DECLS

#if PROTOBUF_C_VERSION_NUMBER < 1003000
# error This file was generated by a newer version of protobuf-c which is incompatible with your libprotobuf-c headers. Please update your headers.
#elif 1005002 < PROTOBUF_C_MIN_COMPILER_VERSION
# error This file was generated by an older version of protobuf-c which is incompatible with your libprotobuf-c headers. Please regenerate this file with a newer version of protobuf-c.
#endif

#include "surrealdb/protocol/v1/value.pb-c.h"

typedef struct Surrealdb__Protocol__V1__ErrorDetails Surrealdb__Protocol__V1__ErrorDetails;
typedef struct Surrealdb__Protocol__V1__RetryHint Surrealdb__Protocol__V1__RetryHint;
typedef struct Surrealdb__Protocol__V1__SurrealError Surrealdb__Protocol__V1__SurrealError;


/* --- enums --- */

/*
 * The category of an error.
 * Mirrors `ErrorDetails` in the server's `surrealdb-types` crate and
 * `ErrorKind` in the JavaScript SDK, variant for variant, because the server
 * already ships that taxonomy over the CBOR and flatbuffers wires and the JS
 * SDK already parses it. A third spelling would guarantee drift.
 * Decoders MUST preserve an unrecognised numeric value rather than normalising
 * it to `UNSPECIFIED`, and MUST treat it as `INTERNAL` for behavioural
 * purposes. This matches the server's `#[surreal(other)] Internal` catch-all
 * and keeps a newer server's error kinds from being misread as "no error kind".
 */
typedef enum _Surrealdb__Protocol__V1__ErrorKind {
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_UNSPECIFIED = 0,
  /*
   * Parse error, invalid request, or invalid params.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_VALIDATION = 1,
  /*
   * A feature or configuration is not supported.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_CONFIGURATION = 2,
  /*
   * Query execution failed: timed out, cancelled, not executed, or a
   * transaction conflict.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_QUERY = 3,
  /*
   * Serialization or deserialization failed.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_SERIALIZATION = 4,
  /*
   * Permission or authorization failure.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_NOT_ALLOWED = 5,
  /*
   * The requested resource does not exist.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_NOT_FOUND = 6,
  /*
   * The resource already exists.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_ALREADY_EXISTS = 7,
  /*
   * Client-side connection failure.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_CONNECTION = 8,
  /*
   * Raised by `THROW` in SurrealQL.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_THROWN = 9,
  /*
   * Internal or unexpected failure. Also the behavioural fallback for a kind
   * this build does not recognise.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_INTERNAL = 10,
  /*
   * Pure chaining context; carries no detail of its own.
   */
  SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_CONTEXT = 11
    PROTOBUF_C__FORCE_ENUM_TO_BE_INT_SIZE(SURREALDB__PROTOCOL__V1__ERROR_KIND)
} Surrealdb__Protocol__V1__ErrorKind;

/* --- messages --- */

/*
 * The specific reason behind an error, within its `ErrorKind`.
 * Deliberately open rather than a mirror of the server's per-kind detail
 * enums. Those are `#[non_exhaustive]` and grow with the engine; reproducing
 * all eight of them here would make every new server-side variant a protocol
 * change, which is exactly the coupling this schema is trying to remove. The
 * server already serialises details as a value, so nothing is lost.
 * Clients MUST NOT branch on `reason` for control flow that has a first-class
 * representation elsewhere — notably retryability, which is `SurrealError.retry`.
 */
struct  Surrealdb__Protocol__V1__ErrorDetails
{
  ProtobufCMessage base;
  /*
   * The reason name, as the server spells it: for example "TransactionConflict",
   * "TimedOut", "Parse", "Thrown". Empty when the kind carries no detail.
   */
  char *reason;
  /*
   * The reason's payload, if it has one. For example `TimedOut` carries the
   * duration after which the query gave up.
   */
  Surrealdb__Protocol__V1__Value *content;
};
#define SURREALDB__PROTOCOL__V1__ERROR_DETAILS__INIT \
 { PROTOBUF_C_MESSAGE_INIT (&surrealdb__protocol__v1__error_details__descriptor) \
, (char *)protobuf_c_empty_string, NULL }


/*
 * Advice about retrying the operation that failed.
 * Presence means the failure is transient and the operation MAY be retried;
 * absence means it MUST NOT be assumed retryable. Presence is the signal
 * rather than a boolean so that "not retryable" and "the server did not say"
 * cannot be confused, which a proto3 `bool` cannot express.
 * This is what makes a transaction conflict structurally detectable. Clients
 * MUST use it instead of matching on `message` or on the legacy numeric
 * `code`; the JS SDK's current reliance on code -32009 (which its own
 * `CODE_TO_KIND` table omits, so conflicts surface as `Internal`) is the bug
 * this field exists to prevent.
 */
struct  Surrealdb__Protocol__V1__RetryHint
{
  ProtobufCMessage base;
  /*
   * How long the client SHOULD wait before retrying. Absent or zero means
   * the client chooses, typically with its own backoff.
   */
  Surrealdb__Protocol__V1__Duration *after;
};
#define SURREALDB__PROTOCOL__V1__RETRY_HINT__INIT \
 { PROTOBUF_C_MESSAGE_INIT (&surrealdb__protocol__v1__retry_hint__descriptor) \
, NULL }


/*
 * A structured SurrealDB error.
 * One error type for the whole protocol: transport-level failures,
 * per-statement query failures, and streaming-export failures all use this
 * message, so a client has one thing to parse and one taxonomy to branch on.
 */
struct  Surrealdb__Protocol__V1__SurrealError
{
  ProtobufCMessage base;
  /*
   * The error category. Always set on a well-formed message.
   */
  Surrealdb__Protocol__V1__ErrorKind kind;
  /*
   * Human-readable, for logs and for showing a user.
   * Clients MUST NOT parse this or branch on it. Everything a client needs
   * to make a decision is in `kind`, `details`, and `retry`; the wording of
   * this string is not part of the contract and will change.
   */
  char *message;
  /*
   * The legacy JSON-RPC numeric code, carried only so clients that predate
   * the structured model keep working. 0 means "not supplied".
   * New clients MUST NOT branch on this. It cannot express the taxonomy
   * faithfully -- several distinct kinds share -32000 -- which is why the
   * structured fields exist.
   */
  int64_t code;
  /*
   * The specific reason, if the server supplied one.
   */
  Surrealdb__Protocol__V1__ErrorDetails *details;
  /*
   * The error that caused this one, innermost last.
   * Recursive message fields are legal in proto3; prost boxes them and
   * ts-proto emits a recursive interface. Servers SHOULD bound the depth of
   * the chain they emit, and clients SHOULD bound the depth they walk.
   */
  Surrealdb__Protocol__V1__SurrealError *cause;
  /*
   * Present when the operation may be retried. See `RetryHint`.
   */
  Surrealdb__Protocol__V1__RetryHint *retry;
};
#define SURREALDB__PROTOCOL__V1__SURREAL_ERROR__INIT \
 { PROTOBUF_C_MESSAGE_INIT (&surrealdb__protocol__v1__surreal_error__descriptor) \
, SURREALDB__PROTOCOL__V1__ERROR_KIND__ERROR_KIND_UNSPECIFIED, (char *)protobuf_c_empty_string, 0, NULL, NULL, NULL }


/* Surrealdb__Protocol__V1__ErrorDetails methods */
void   surrealdb__protocol__v1__error_details__init
                     (Surrealdb__Protocol__V1__ErrorDetails         *message);
size_t surrealdb__protocol__v1__error_details__get_packed_size
                     (const Surrealdb__Protocol__V1__ErrorDetails   *message);
size_t surrealdb__protocol__v1__error_details__pack
                     (const Surrealdb__Protocol__V1__ErrorDetails   *message,
                      uint8_t             *out);
size_t surrealdb__protocol__v1__error_details__pack_to_buffer
                     (const Surrealdb__Protocol__V1__ErrorDetails   *message,
                      ProtobufCBuffer     *buffer);
Surrealdb__Protocol__V1__ErrorDetails *
       surrealdb__protocol__v1__error_details__unpack
                     (ProtobufCAllocator  *allocator,
                      size_t               len,
                      const uint8_t       *data);
void   surrealdb__protocol__v1__error_details__free_unpacked
                     (Surrealdb__Protocol__V1__ErrorDetails *message,
                      ProtobufCAllocator *allocator);
/* Surrealdb__Protocol__V1__RetryHint methods */
void   surrealdb__protocol__v1__retry_hint__init
                     (Surrealdb__Protocol__V1__RetryHint         *message);
size_t surrealdb__protocol__v1__retry_hint__get_packed_size
                     (const Surrealdb__Protocol__V1__RetryHint   *message);
size_t surrealdb__protocol__v1__retry_hint__pack
                     (const Surrealdb__Protocol__V1__RetryHint   *message,
                      uint8_t             *out);
size_t surrealdb__protocol__v1__retry_hint__pack_to_buffer
                     (const Surrealdb__Protocol__V1__RetryHint   *message,
                      ProtobufCBuffer     *buffer);
Surrealdb__Protocol__V1__RetryHint *
       surrealdb__protocol__v1__retry_hint__unpack
                     (ProtobufCAllocator  *allocator,
                      size_t               len,
                      const uint8_t       *data);
void   surrealdb__protocol__v1__retry_hint__free_unpacked
                     (Surrealdb__Protocol__V1__RetryHint *message,
                      ProtobufCAllocator *allocator);
/* Surrealdb__Protocol__V1__SurrealError methods */
void   surrealdb__protocol__v1__surreal_error__init
                     (Surrealdb__Protocol__V1__SurrealError         *message);
size_t surrealdb__protocol__v1__surreal_error__get_packed_size
                     (const Surrealdb__Protocol__V1__SurrealError   *message);
size_t surrealdb__protocol__v1__surreal_error__pack
                     (const Surrealdb__Protocol__V1__SurrealError   *message,
                      uint8_t             *out);
size_t surrealdb__protocol__v1__surreal_error__pack_to_buffer
                     (const Surrealdb__Protocol__V1__SurrealError   *message,
                      ProtobufCBuffer     *buffer);
Surrealdb__Protocol__V1__SurrealError *
       surrealdb__protocol__v1__surreal_error__unpack
                     (ProtobufCAllocator  *allocator,
                      size_t               len,
                      const uint8_t       *data);
void   surrealdb__protocol__v1__surreal_error__free_unpacked
                     (Surrealdb__Protocol__V1__SurrealError *message,
                      ProtobufCAllocator *allocator);
/* --- per-message closures --- */

typedef void (*Surrealdb__Protocol__V1__ErrorDetails_Closure)
                 (const Surrealdb__Protocol__V1__ErrorDetails *message,
                  void *closure_data);
typedef void (*Surrealdb__Protocol__V1__RetryHint_Closure)
                 (const Surrealdb__Protocol__V1__RetryHint *message,
                  void *closure_data);
typedef void (*Surrealdb__Protocol__V1__SurrealError_Closure)
                 (const Surrealdb__Protocol__V1__SurrealError *message,
                  void *closure_data);

/* --- services --- */


/* --- descriptors --- */

extern const ProtobufCEnumDescriptor    surrealdb__protocol__v1__error_kind__descriptor;
extern const ProtobufCMessageDescriptor surrealdb__protocol__v1__error_details__descriptor;
extern const ProtobufCMessageDescriptor surrealdb__protocol__v1__retry_hint__descriptor;
extern const ProtobufCMessageDescriptor surrealdb__protocol__v1__surreal_error__descriptor;

PROTOBUF_C__END_DECLS


#endif  /* PROTOBUF_C_surrealdb_2fprotocol_2fv1_2ferror_2eproto__INCLUDED */
